{
  "name": "react-starter",
  "license": "ISC",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "npm run check && parcel",
    "prebuild": "rimraf dist",
    "build": "npm run check && parcel build",
    "check": "tsc --skipLibCheck --noEmit",
    "test": "npm run check && jest"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.23.0",
    "@babel/preset-typescript": "^7.23.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "babel-jest": "^29.7.0",
    "jest": "^29.7.0",
    "parcel": "^2.10.0",
    "process": "^0.11.10",
    "rimraf": "^5.0.0",
    "typescript": "^5.2.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
